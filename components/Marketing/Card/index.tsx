import React from 'react';
import classes from './Card.module.scss';


const Card = () => {
	return (
		<div className={classes.Card} style={{ backgroundImage: `url(black-firday.jpg)` }}>

		</div>
	);
};

export default Card;
